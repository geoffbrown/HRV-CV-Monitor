import Foundation

// TEMPLATE — this file IS committed; the real one is not.
//
// Setup on a fresh clone:
//   1. Copy this file to  "HRV-CV Monitor/Secrets.swift"
//   2. Paste your WHOOP Client Secret below
//      (developer dashboard → CLIENT SECRET → eye icon)
//
// Secrets.swift is git-ignored, so your secret stays on your machine only.
enum Secrets {
    static let whoopClientSecret = "PASTE_YOUR_WHOOP_CLIENT_SECRET_HERE"
}
